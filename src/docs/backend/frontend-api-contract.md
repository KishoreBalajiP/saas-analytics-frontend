# Frontend API Contract — SaaS Analytics Platform

> **Source of truth:** this document was generated by inspecting every route, controller, service, validator, and model in the actual working tree (`D:\saas-analytics-platform\saas-analytics-backend`). Nothing is inferred from documentation alone.
> **Backend status:** 405 tests pass, 5/5 CI guards green, `npm audit` 2 moderate (transitive uuid in exceljs, acceptable).
> **Base URL (development):** `http://localhost:8080`
> **All paths are prefixed with `/api/v1`** unless otherwise noted.
> **Response envelope (all endpoints):**
>
> ```json
> {
>   "success": true,
>   "statusCode": 200,
>   "message": "Human-readable status",
>   "data": { ... },
>   "meta": { ... },
>   "timestamp": "2026-08-16T12:00:00.000Z"
> }
> ```
>
> **Error envelope:**
>
> ```json
> {
>   "success": false,
>   "statusCode": 401,
>   "message": "Invalid or expired token",
>   "code": "INVALID_TOKEN",
>   "errors": [{ "field": "email", "message": "email is required" }],
>   "timestamp": "..."
> }
> ```
>
> **Pagination shape (when `meta` carries pagination):**
>
> ```json
> "meta": { "page": 1, "limit": 20, "total": 142, "pages": 8 }
> ```
>
> Lists return `data` as an array; the wrapper fields `page`, `limit`, `total`, `pages` live in `meta`.

---

## Table of Contents

1. [Authentication & Sessions](#1-authentication--sessions)
2. [Admin Authentication & MFA](#2-admin-authentication--mfa)
3. [Tenant Portal — Profile](#3-tenant-portal--profile)
4. [Admin — Tenants](#4-admin--tenants)
5. [Admin — Platform Admins](#5-admin--platform-admins)
6. [Admin — Roles & Permissions](#6-admin--roles--permissions)
7. [Connectors (CSV / XLSX / MongoDB / Webhook)](#7-connectors-csv--xlsx--mongodb--webhook)
8. [Analytics & Query History](#8-analytics--query-history)
9. [Dashboards & Widgets](#9-dashboards--widgets)
10. [Reports](#10-reports)
11. [Alerts](#11-alerts)
12. [Notifications](#12-notifications)
13. [Master Data (Public Reference Catalogues)](#13-master-data-public-reference-catalogues)
14. [Monitoring (Admin)](#14-monitoring-admin)
15. [Audit Logs (Admin)](#15-audit-logs-admin)
16. [Access Logs (Admin)](#16-access-logs-admin)
17. [Compliance (Admin + Subject-Facing)](#17-compliance-admin--subject-facing)
18. [Support (Admin)](#18-support-admin)
19. [API Keys (Tenant)](#19-api-keys-tenant)
20. [Embed Tokens (Tenant + Public)](#20-embed-tokens-tenant--public)
21. [External API (`X-Api-Key`)](#21-external-api-x-api-key)
22. [Webhooks (Inbound, — Provider HMAC)](#22-webhooks-inbound--provider-hmac)

---

## 1. Authentication & Sessions

All endpoints mounted under `/auth`. All public credential endpoints are `strictLimiter`-throttled.

### `POST /auth/login`

- **Authentication:** none (public, throttled)
- **Headers:** `X-Tenant-Id: <tenantSlug>` (required for tenant login)
- **Request:**

```json
{
  "email": "user@example.com",
  "password": "string (>= 8 chars)",
  "mfaToken": "123456" // optional, 6 digits
}
```

- **Response 200:**

```json
{
  "success": true,
  "data": {
    "accessToken": "eyJ...",
    "expiresIn": 900,
    "refreshToken": "rt_...",
    "sessionId": "ses_01H...",
    "actor": {
      "id": "66a...",
      "email": "user@example.com",
      "tenantId": "<tenantSlug>",
      "role": "Owner"
    }
  }
}
```

- **Side effect:** `Set-Cookie: saas_session=<refreshToken>; HttpOnly; Secure; SameSite=Lax; Max-Age=2592000`
- **Errors:** 401 invalid credentials (generic), 403 tenant not active, 422 validation failed, 429 too many attempts, 423 account locked
- **Permission:** none (public)
- **Frontend notes:** store `accessToken` in memory; backend sets refresh as HttpOnly cookie. Use the cookie for `/auth/refresh` automatically.

### `POST /auth/refresh`

- **Authentication:** refresh token via `Cookie: saas_session=…` OR `body.refreshToken`
- **Response 200:** identical shape to `/auth/login` (rotates refresh token).
- **Errors:** 401 invalid/expired refresh (family-revoked if replayed)

### `POST /auth/logout`

- **Authentication:** none required (cookie or body token)
- **Response 200:** `{ "success": true }`; clears `saas_session` cookie.
- **Frontend notes:** call on every logout button; discards access token client-side.

### `GET /auth/me`

- **Authentication:** Bearer access token (`authenticate` middleware)
- **Response 200 `data`:**

```json
{
  "id": "66a...",
  "email": "user@example.com",
  "tenantId": "<tenantSlug>",
  "status": "active",
  "profile": { "name": "Jane Doe", "locale": "en", "timezone": "UTC" },
  "roles": ["Owner"],
  "permissions": ["dashboards.view", "dashboards.create", ...]
}
```

- **Errors:** 401 missing/invalid token, 403 suspended session

### `POST /auth/password/forgot`

- **Authentication:** none (rate-limited)
- **Headers:** `X-Tenant-Id`
- **Request:** `{ "email": "user@example.com" }`
- **Response 200:** always `{ ok: true }` (no enumeration)

### `POST /auth/password/reset`

- **Authentication:** none (stateless emailed token)
- **Headers:** `X-Tenant-Id`
- **Request:** `{ "token": "...", "newPassword": "string >= 8 chars" }`
- **Response 200:** `{ ok: true }`; revokes all sessions for the user.

---

## 2. Admin Authentication & MFA

All endpoints mounted under `/admin-auth`. Same shape as tenant auth, except the actor is an `Admin` not a `User`, and MFA enrolment is exposed.

### `POST /admin-auth/login`

- **Request:** same as `/auth/login` (no `X-Tenant-Id`).
- **Note:** for `super_admin` admins, the MFA step may be required; the response indicates `mfaRequired: true` with `{ mfaToken }` to retry with `mfaToken`.

### `POST /admin-auth/refresh` / `POST /admin-auth/logout` / `GET /admin-auth/me`

- Identical to tenant auth, returns admin profile.

### `POST /admin-auth/mfa/enroll`

- **Authentication:** `adminAuth` (Bearer admin token)
- **Response 200:** `{ secret: "BASE32…", otpauthUrl: "otpauth://totp/…", qrCodeDataUrl: "data:image/png;base64,…" }`

### `POST /admin-auth/mfa/verify`

- **Authentication:** `adminAuth`
- **Request:** `{ "code": "123456" }`
- **Response 200:** `{ "verified": true }`

---

## 3. Tenant Portal — Profile

All endpoints mounted under `/users`.

### `GET /users/me`

- **Auth:** Bearer
- **Response:** full profile incl. `tenantId`, `roles[]`, `permissions[]`, `mfaEnabled`

### `PATCH /users/me`

- **Auth:** Bearer
- **Request:** `{ name?, timezone?, locale?, avatarUrl? }`
- **Response 200:** updated profile

### `GET /users`

- **Auth:** Bearer + `users.view`
- **Query:** `page, limit (<= 100), status, search, role`
- **Response:** `data: User[]` + pagination meta

### `GET /users/:userId`

- **Auth:** Bearer + `users.view`
- **Response:** `data: User` (404 if cross-tenant)

---

## 4. Admin — Tenants

All endpoints mounted under `/tenants`. All require **admin Bearer token** + `iam.tenants.<action>` permission.

| Method | Path                    | Permission              | Notes                                                     |
| ------ | ----------------------- | ----------------------- | --------------------------------------------------------- |
| POST   | `/tenants`              | `iam.tenants.create`    | body: `{ slug, name, owner: { email, name, password? } }` |
| GET    | `/tenants`              | `iam.tenants.view`      | paged; `status, search` query                             |
| GET    | `/tenants/:id`          | `iam.tenants.view`      | detail                                                    |
| PATCH  | `/tenants/:id`          | `iam.tenants.update`    | name/description only                                     |
| POST   | `/tenants/:id/suspend`  | `iam.tenants.suspend`   | `{ reason }`                                              |
| POST   | `/tenants/:id/restore`  | `iam.tenants.restore`   |                                                           |
| POST   | `/tenants/:id/disable`  | `iam.tenants.suspend`   |                                                           |
| POST   | `/tenants/:id/archive`  | `iam.tenants.delete`    | terminal                                                  |
| POST   | `/tenants/:id/init`     | `iam.tenants.create`    | idempotent onboarding                                     |
| GET    | `/tenants/:id/members`  | `iam.tenants.view`      | owner + role assignments                                  |
| GET    | `/tenants/:id/stats`    | `iam.tenants.view`      | counts (users, sessions, connectors, rows)                |
| GET    | `/tenants/:id/billing`  | `iam.tenants.view`      | plan + usage                                              |
| GET    | `/tenants/:id/settings` | `iam.tenants.view`      | `?group=`                                                 |
| PATCH  | `/tenants/:id/settings` | `iam.tenants.configure` | upsert                                                    |
| POST   | `/tenants/:id/owner`    | `iam.tenants.assign`    | `{ userId }`                                              |

### Tenant model fields

```json
{
  "_id": "66a...",
  "slug": "acme",
  "name": "Acme Corp",
  "description": "...",
  "status": "active", // pending | active | suspended | disabled | archived
  "onboardingStatus": "ready",
  "ownerId": "66a...",
  "createdAt": "...",
  "updatedAt": "..."
}
```

---

## 5. Admin — Platform Admins

Mounted under `/admin/admins`. Requires **admin Bearer** + `iam.admins.<action>`.

| Method | Path                              | Permission                                                    |
| ------ | --------------------------------- | ------------------------------------------------------------- |
| GET    | `/admin/admins`                   | `view`                                                        |
| POST   | `/admin/admins`                   | `create` — body `{ email, password, name, adminType, scope }` |
| GET    | `/admin/admins/:id`               | `view`                                                        |
| PATCH  | `/admin/admins/:id`               | `update`                                                      |
| POST   | `/admin/admins/:id/suspend`       | `suspend` — body `{ reason }`                                 |
| POST   | `/admin/admins/:id/restore`       | `restore`                                                     |
| POST   | `/admin/admins/:id/roles`         | `assign` — body `{ roleId, expiresAt? }`                      |
| DELETE | `/admin/admins/:id/roles/:roleId` | `assign`                                                      |
| GET    | `/admin/admins/:id/audit`         | `view`                                                        |

`adminType`: `super_admin | platform_admin | support_admin | readonly_admin`.

---

## 6. Admin — Roles & Permissions

Mounted under `/roles` and `/permissions`. All admin Bearer + `iam.roles.*` / `iam.permissions.*`.

### `/roles`

| Method | Path                     | Body                                                                  | Permission                   |
| ------ | ------------------------ | --------------------------------------------------------------------- | ---------------------------- |
| GET    | `/roles`                 | —                                                                     | view; query: `scope=platform | tenant` |
| POST   | `/roles`                 | `{ name, description, scope, permissions: ['dashboards.view', ...] }` | create                       |
| GET    | `/roles/:id`             | —                                                                     | view                         |
| PATCH  | `/roles/:id`             | `{ name?, description? }`                                             | update                       |
| DELETE | `/roles/:id`             | —                                                                     | delete (409 if assigned)     |
| POST   | `/roles/:id/permissions` | `{ permissionKey: "dashboards.view" }`                                | assign                       |
| DELETE | `/roles/:id/permissions` | `{ permissionKey }`                                                   | assign                       |

### `/permissions`

| Method | Path                                | Body                                   | Permission |
| ------ | ----------------------------------- | -------------------------------------- | ---------- |
| GET    | `/permissions`                      | —                                      | view       |
| POST   | `/permissions`                      | `{ module, action, description? }`     | create     |
| DELETE | `/permissions`                      | `{ permissionKey }`                    | delete     |
| GET    | `/permissions/modules`              | —                                      | view       |
| POST   | `/permissions/modules`              | `{ key, name, parentKey? }`            | create     |
| GET    | `/permissions/modules/:key/actions` | —                                      | view       |
| POST   | `/permissions/bulk`                 | `{ items: [{ module, action }, ...] }` | create     |

Canonical actions: `view, create, update, delete, export, approve, suspend, restore, assign, configure, evaluate, impersonate, recover, broadcast, lookup, file, cancel`.

---

## 7. Connectors (CSV / XLSX / MongoDB / Webhook)

Mounted under `/connectors`. All routes require **Bearer + `connectors.<action>`**.

### Common

| Method | Path                                | Permission                                                                          |
| ------ | ----------------------------------- | ----------------------------------------------------------------------------------- |
| GET    | `/connectors/types`                 | `connectors.view` — catalogue of registered providers                               |
| GET    | `/connectors`                       | `connectors.view` — paged (`type, status, search, page, limit`)                     |
| POST   | `/connectors`                       | `connectors.create` — `{ type, name, config, fieldMapping? }`                       |
| GET    | `/connectors/:connectorId`          | `connectors.view`                                                                   |
| PATCH  | `/connectors/:connectorId`          | `connectors.update` — `{ name?, status?, config?, fieldMapping? }`; type immutable  |
| DELETE | `/connectors/:connectorId`          | `connectors.delete` — soft delete (204)                                             |
| POST   | `/connectors/:connectorId/validate` | `connectors.view` — 200 if valid, 422 with `{ valid:false, errors:[…], connector }` |
| GET    | `/connectors/:connectorId/rows`     | `connectors.view` — paged ingested rows                                             |

### CSV upload

- `POST /connectors/:connectorId/preview` (`connectors.preview`, multipart `file` field, .csv/.txt, ≤ 10 MB) → `{ fields: string[], sample: Array, meta: { delimiter, hasHeader } }`
- `POST /connectors/:connectorId/sync` (`connectors.sync`, multipart `file`) → 202 `{ accepted: true, jobType: 'ingest', filename }` (processed async via connector queue)

### XLSX upload

- `POST /connectors/:connectorId/preview` (multipart `file`, .xlsx/.xls, ≤ 10 MB) → `{ fields, sample, meta: { sheetName, rowCount, hasHeader } }`
- `POST /connectors/:connectorId/sync` (multipart `file`) → 202 `{ accepted: true, jobType: 'ingest', filename }`

### MongoDB pull-sync

- `POST /connectors/:connectorId/sync-mongodb` (no file, JSON body) → 202 `{ accepted: true, jobType: 'ingest' }`

### Webhook inbound

- `POST /webhooks/:webhookToken` — **public, ci:routes-exempt**, HMAC-signed body:
  - Headers: `X-Saas-Signature: sha256=<hex>`, `X-Saas-Timestamp: <unix-seconds>` (optional if `requireTimestamp: false`)
  - Body: raw JSON bytes
  - 202 accepted, 401 bad signature, 422 bad JSON

### Connector model (redacted for API)

```json
{
  "_id": "66a...",
  "type": "csv" | "xlsx" | "mongodb" | "webhook",
  "name": "Sales Q3",
  "status": "active" | "paused" | "error",
  "configSummary": { /* type-dependent, secrets never included */ },
  "fieldMapping": { "...": "..." },
  "webhookToken": "whk_...",   // webhook type only
  "lastSyncedAt": "...",
  "lastError": null,
  "errorCount": 0,
  "createdAt": "...",
  "updatedAt": "..."
}
```

### Connector type catalogue (`GET /connectors/types`)

```json
[
  {
    "type": "csv",
    "displayName": "CSV Import",
    "description": "...",
    "capabilities": ["validate", "preview", "ingest"]
  },
  {
    "type": "xlsx",
    "displayName": "XLSX Import",
    "description": "...",
    "capabilities": ["validate", "preview", "ingest"]
  },
  {
    "type": "mongodb",
    "displayName": "External MongoDB",
    "description": "...",
    "capabilities": ["validate", "ingest"]
  },
  {
    "type": "webhook",
    "displayName": "Inbound Webhook",
    "description": "...",
    "capabilities": ["validate", "ingest"]
  }
]
```

### Connector config shapes (POST/PATCH `/connectors`)

- **csv:** `{ delimiter?: string (1 char), hasHeader?: boolean }`
- **xlsx:** `{ hasHeader?: boolean, sheet?: string|number }`
- **mongodb:** `{ uri: string (mongodb://…), database: string, collection: string, filter?: object }`
- **webhook:** `{ signingSecret: string (>= 16 chars), toleranceSeconds?: number, requireTimestamp?: boolean }`

### Connector redaction (what `configSummary` returns)

- **csv:** `{ delimiter, hasHeader }`
- **xlsx:** `{ hasHeader, sheet }`
- **mongodb:** `{ host, database, collection, filterConfigured, hasCredentials }` — **no URI, no credentials**
- **webhook:** `{ toleranceSeconds, requireTimestamp, signingSecretConfigured }` — **no secret**

---

## 8. Analytics & Query History

Mounted under `/analytics`. All routes Bearer + `analytics.<action>`.

### `GET /analytics`

- **Auth:** Bearer + `analytics.view`
- **Query (all optional):**
  - `connectorIds` — comma-separated ObjectIds or array
  - `filters` — JSON string array `[{ field, op, value }]`
  - `filtersOp` — `'and' | 'or'` (default `'and'`)
  - `dateRange` — JSON `{ from: ISO, to: ISO }`
  - `metrics` — JSON array `[{ field, op, alias }]` where op ∈ `count, sum, avg, min, max`
  - `groupBy` — JSON array of field names (empty/missing = raw rows)
  - `orderBy` — JSON `[{ field, dir }]`
  - `page`, `limit` (≤ 200, default 50)
- **Response 200 `data`:** `rows: Array<object>`, `meta` carries `total, page, pages, limit, columns, cached, executedAt, groupMode ('grouped' | 'raw'), queryId, cacheKey`.

### `GET /analytics/queries`

- **Auth:** Bearer + `analytics.view`
- **Query:** `page, limit (<= 100), status`
- **Response:** `data: AnalyticsQuery[]` + pagination meta

### `GET /analytics/queries/:id`

- **Auth:** Bearer + `analytics.view`
- **Response:** `data: AnalyticsQuery`

### `POST /analytics/export`

- **Auth:** Bearer + `analytics.export`
- **Body:** same params as `GET /analytics` minus pagination, plus `format: 'json'|'csv'|'xlsx'`
- **Response 202:** `{ accepted: true, exportId, status: 'queued' }` (poll via `GET /audit-logs/export/:exportId` or connector status)

---

## 9. Dashboards & Widgets

Mounted under `/dashboards`. All routes Bearer + tenant resolution + `dashboards.<action>`. Widget execution additionally requires `analytics.view`.

### Dashboards

| Method | Path                             | Permission            | Body                                                            |
| ------ | -------------------------------- | --------------------- | --------------------------------------------------------------- |
| GET    | `/dashboards`                    | view                  | —                                                               |
| POST   | `/dashboards`                    | create                | `{ name, description?, layout?, filters?, refresh? }`           |
| GET    | `/dashboards/:id`                | view                  | `?includeWidgets=true                                           | false` (default true) |
| PATCH  | `/dashboards/:id`                | update                | `{ name?, description?, status?, layout?, filters?, refresh? }` |
| POST   | `/dashboards/:id/publish`        | update                | —                                                               |
| POST   | `/dashboards/:id/duplicate`      | update                | —                                                               |
| POST   | `/dashboards/:id/share`          | update                | `{ email, role: 'viewer', expiresAt? }`                         |
| DELETE | `/dashboards/:id/share/:entryId` | update                | —                                                               |
| DELETE | `/dashboards/:id`                | delete                | — (cascades to widgets)                                         |
| GET    | `/dashboards/:id/execute`        | view + analytics.view | —                                                               |

### Widgets

| Method | Path                                        | Permission            | Body                                                              |
| ------ | ------------------------------------------- | --------------------- | ----------------------------------------------------------------- |
| GET    | `/dashboards/:id/widgets`                   | view                  | — (paged)                                                         |
| POST   | `/dashboards/:id/widgets`                   | update                | `{ name, type, datasetId, query?, visualization?, position? }`    |
| GET    | `/dashboards/:id/widgets/:widgetId`         | view                  | —                                                                 |
| PATCH  | `/dashboards/:id/widgets/:widgetId`         | update                | `{ name?, type?, datasetId?, query?, visualization?, position? }` |
| DELETE | `/dashboards/:id/widgets/:widgetId`         | update                | —                                                                 |
| GET    | `/dashboards/:id/widgets/:widgetId/execute` | view + analytics.view | —                                                                 |

`type` ∈ `kpi | table | bar | line | area | pie`. `datasetId` is the connector `_id` (the "dataset" = a connector bucket).

### Dashboard model

```json
{
  "_id": "66a...",
  "tenantId": "<slug>",
  "name": "Sales Dashboard",
  "description": "...",
  "status": "draft | published | archived",
  "layout": {/* dashboard-level layout */},
  "filters": {/* dashboard-level date/filter overrides */},
  "refresh": {/* polling config */},
  "shares": [{ "id": "66a...", "email": "...", "role": "viewer", "expiresAt": null }],
  "createdAt": "...",
  "updatedAt": "..."
}
```

### Widget model

```json
{
  "_id": "66a...",
  "dashboardId": "66a...",
  "type": "kpi | table | bar | line | area | pie",
  "name": "Revenue by Region",
  "datasetId": "66a...",
  "query": {/* safe-contract whitelisted query */},
  "visualization": {/* chart options */},
  "position": { "x": 0, "y": 0, "w": 6, "h": 4 }
}
```

### Dashboard execution response (`GET /dashboards/:id/execute`)

- **Response 200:** `data: [{ widgetId, name, type, result: <analyticsEngineResult>, error?: { message, code } }]`, `meta: { dashboardId, name, widgetCount }`. Per-widget partial failures are allowed.

### Widget execution response

- **Response 200:** `data: <analyticsEngineResult>` (`{ rows, total, page, pages, limit, columns, executedAt, groupMode }`), `meta: { widgetId, name, type, datasetId, cached, cacheKey }`.

---

## 10. Reports

Mounted under `/reports`. Bearer + `reports.<action>`.

| Method | Path                    | Permission                                                     |
| ------ | ----------------------- | -------------------------------------------------------------- |
| GET    | `/reports`              | view — query: `page, limit, status, search`                    |
| POST   | `/reports`              | create                                                         |
| GET    | `/reports/:id`          | view (includes `runs[]` history)                               |
| PATCH  | `/reports/:id`          | update                                                         |
| POST   | `/reports/:id/run`      | export — `{ format?, filters? }` → 202                         |
| DELETE | `/reports/:id`          | delete                                                         |
| GET    | `/reports/:id/download` | export — `?runId=` → `{ url, expiresAt }` (signed storage URL) |

### Report model

```json
{
  "_id": "66a...",
  "name": "Weekly Sales Report",
  "source": "widget | query",
  "sourceWidgetId": "66a...",
  "sourceQuery": {/* safe contract */},
  "format": "json | csv | xlsx",
  "schedule": { "enabled": true, "cron": "0 8 * * 1", "timezone": "UTC", "recipients": ["..."] },
  "nextRunAt": "...",
  "runs": [
    {
      "runId": "r_...",
      "triggeredBy": "manual | schedule",
      "format": "csv",
      "status": "queued | running | ready | failed",
      "rowCount": 142,
      "resultKey": "exports/...",
      "startedAt": "...",
      "finishedAt": "..."
    }
  ]
}
```

---

## 11. Alerts

Mounted under `/alerts`. Bearer + `alerts.<action>`.

| Method | Path                   | Permission                                            |
| ------ | ---------------------- | ----------------------------------------------------- |
| GET    | `/alerts`              | view — query: `page, limit, enabled`                  |
| POST   | `/alerts`              | create                                                |
| GET    | `/alerts/events`       | view — all tenant events                              |
| GET    | `/alerts/:id`          | view                                                  |
| PATCH  | `/alerts/:id`          | update                                                |
| DELETE | `/alerts/:id`          | delete                                                |
| POST   | `/alerts/:id/evaluate` | evaluate — manual; `{ triggered, event?, disabled? }` |
| GET    | `/alerts/:id/events`   | view — paged                                          |

### AlertRule model

```json
{
  "_id": "66a...",
  "name": "Daily revenue drop",
  "datasetId": "66a...",
  "condition": { "field": "revenue", "op": "lt", "value": 1000 },
  "source": "widget | query",
  "sourceWidgetId": "...",
  "sourceQuery": {/* safe contract */},
  "schedule": { "cron": "0 9 * * *", "timezone": "UTC" },
  "cooldownMinutes": 60,
  "lastTriggeredAt": null,
  "nextEvaluationAt": "...",
  "enabled": true,
  "notification": { "channels": ["email", "in_app"], "recipients": ["..."], "template": "..." }
}
```

### AlertEvent model

```json
{
  "_id": "66a...",
  "alertId": "...",
  "triggeredAt": "...",
  "value": 842.5,
  "threshold": 1000,
  "message": "...",
  "status": "triggered | suppressed | resolved",
  "notificationIds": ["..."]
}
```

---

## 12. Notifications

Mounted under `/notifications`. Bearer + `notifications.<action>`.

| Method | Path                          | Permission                                                |
| ------ | ----------------------------- | --------------------------------------------------------- |
| GET    | `/notifications`              | view — `page, limit, unreadOnly`                          |
| GET    | `/notifications/unread-count` | view → `{ count }`                                        |
| GET    | `/notifications/preferences`  | view                                                      |
| POST   | `/notifications/preferences`  | update — `{ preferences: { email: true, in_app: true } }` |
| POST   | `/notifications/read-all`     | update                                                    |
| POST   | `/notifications/:id/read`     | update                                                    |
| DELETE | `/notifications/:id`          | delete                                                    |

### Notification model

```json
{
  "_id": "66a...",
  "type": "alert.triggered | report.ready | broadcast | system",
  "title": "...",
  "body": "...",
  "data": {/* type-specific payload */},
  "read": false,
  "readAt": null,
  "createdAt": "..."
}
```

---

## 13. Master Data (Public Reference Catalogues)

Mounted under `/master-data`. GETs are public + cached; writes are admin-only.

| Method | Path                             | Permission          |
| ------ | -------------------------------- | ------------------- |
| GET    | `/master-data/:catalogue`        | none (optionalAuth) |
| GET    | `/master-data/:catalogue/:id`    | none                |
| POST   | `/master-data/:catalogue`        | admin only          |
| PATCH  | `/master-data/:catalogue/:id`    | admin only          |
| DELETE | `/master-data/:catalogue/:id`    | admin only          |
| POST   | `/master-data/:catalogue/import` | admin only (501)    |
| POST   | `/master-data/:catalogue/export` | admin only (501)    |

### MasterData model

```json
{ "_id": "66a...", "code": "USD", "locale": "en", "label": "US Dollar", "value": { ... }, "isSystem": false, "createdAt": "..." }
```

---

## 14. Monitoring (Admin)

Mounted under `/monitoring`. Admin Bearer + `monitoring.view`.

| Method | Path                            | Description                   |
| ------ | ------------------------------- | ----------------------------- |
| GET    | `/monitoring/health/system`     | process / event loop / uptime |
| GET    | `/monitoring/health/db`         | mongo ping + replication lag  |
| GET    | `/monitoring/health/websocket`  | socket connection counts      |
| GET    | `/monitoring/health/queue`      | **501 (Phase 3)**             |
| GET    | `/monitoring/health/scheduler`  | **501 (Phase 3)**             |
| GET    | `/monitoring/health/storage`    | **501 (Phase 3)**             |
| GET    | `/monitoring/health/connectors` | **501 (Phase 3)**             |
| GET    | `/monitoring/health/aggregate`  | one-shot summary, 5 s cached  |
| GET    | `/monitoring/metrics`           | **501 (Phase 4 Prometheus)**  |

---

## 15. Audit Logs (Admin)

Mounted under `/audit-logs`. Admin Bearer + `audit_logs.<view|export>`.

| Method | Path                           | Body / Query                                                                       | Permission                            |
| ------ | ------------------------------ | ---------------------------------------------------------------------------------- | ------------------------------------- |
| GET    | `/audit-logs`                  | `actor, module, action, resourceType, resourceId, from, to, tenantId, page, limit` | view                                  |
| GET    | `/audit-logs/modules/:module`  | `page, limit`                                                                      | view                                  |
| GET    | `/audit-logs/:id`              | —                                                                                  | view                                  |
| POST   | `/audit-logs/export`           | `{ filters, format: 'json'                                                         | 'csv' }`→ 202`{ exportId }`           | export |
| GET    | `/audit-logs/export/:exportId` | —                                                                                  | view → `{ status, url?, expiresAt? }` |

### AuditLog model

```json
{
  "_id": "66a...",
  "actor": { "type": "user|admin|system", "id": "..." },
  "module": "connectors",
  "action": "connector.create",
  "resource": { "type": "connector", "id": "66a..." },
  "tenantId": "<slug>",
  "before": {/* sanitized snapshot */},
  "after": {/* sanitized snapshot */},
  "reason": "...",
  "ip": "...",
  "userAgent": "...",
  "createdAt": "..."
}
```

---

## 16. Access Logs (Admin)

Mounted under `/access-logs`. Admin Bearer + `access_logs.<view|export>`.

| Method | Path                            | Permission                                                            |
| ------ | ------------------------------- | --------------------------------------------------------------------- |
| GET    | `/access-logs`                  | view — `actor, method, path, status, tenantId, from, to, page, limit` |
| GET    | `/access-logs/top-paths`        | view — aggregated (capped 100)                                        |
| GET    | `/access-logs/top-errors`       | view — aggregated                                                     |
| POST   | `/access-logs/export`           | export                                                                |
| GET    | `/access-logs/export/:exportId` | view (poll status)                                                    |

---

## 17. Compliance (Admin + Subject-Facing)

Mounted under `/compliance`.

### Admin-gated

| Method | Path                              | Permission                                               |
| ------ | --------------------------------- | -------------------------------------------------------- |
| POST   | `/compliance/requests`            | create — `{ subjectEmail, subjectUserId?, type: 'export' | 'delete' | 'restrict', notes? }` |
| GET    | `/compliance/requests`            | view — `status, type, page, limit`                       |
| GET    | `/compliance/requests/:id`        | view — status + evidence                                 |
| POST   | `/compliance/requests/:id/cancel` | update                                                   |

### Subject-facing

| Method | Path                              | Permission                                         |
| ------ | --------------------------------- | -------------------------------------------------- |
| POST   | `/compliance/public/requests`     | JWT                                                |
| GET    | `/compliance/public/requests/:id` | signed HMAC token (pollToken) — `ci:routes-exempt` |

---

## 18. Support (Admin)

Mounted under `/support`. Admin Bearer + `support.configure`.

| Method | Path                               | Body                                                                                       |
| ------ | ---------------------------------- | ------------------------------------------------------------------------------------------ |
| POST   | `/support/impersonate`             | `{ userId, reason }` — returns short-lived user-scoped bearer token; daily budget enforced |
| POST   | `/support/impersonate/stop`        | `{ sessionId }`                                                                            |
| POST   | `/support/account/recover`         | `{ email, type: 'reset'                                                                    | 'unlock' }` |
| POST   | `/support/account/revoke-sessions` | `{ userId }`                                                                               |
| GET    | `/support/tenants/:id/lookups`     | — (tenant overview + statistics)                                                           |
| POST   | `/support/notifications/broadcast` | `{ title, body, audience: { tenantId?, roles? } }`                                         |

---

## 19. API Keys (Tenant)

Mounted under `/api-keys`. Bearer + `api_keys.<action>`.

| Method | Path                   | Permission                                              |
| ------ | ---------------------- | ------------------------------------------------------- |
| POST   | `/api-keys`            | create — body: `{ name, scopes: string[], expiresAt? }` |
| GET    | `/api-keys`            | view — `page, limit, status`                            |
| GET    | `/api-keys/:id`        | view                                                    |
| PATCH  | `/api-keys/:id`        | update — `{ name?, scopes?, expiresAt? }`               |
| POST   | `/api-keys/:id/revoke` | delete — `{ reason? }`                                  |

### Create response (critical — secret returned ONCE)

```json
{
  "success": true,
  "data": {
    "key": {
      "id": "66a...",
      "prefix": "sak_a1b2c3d4...",
      "name": "Production ingestion",
      "scopes": ["analytics:query", "datasets:read"],
      "expiresAt": "...",
      "status": "active",
      "createdAt": "..."
    },
    "secret": "sak_a1b2c3d4..." // FULL SECRET — only ever shown at creation
  }
}
```

### Scopes (allow-listed)

`analytics:query`, `datasets:read`, `connectors:read`, `dashboards:read`

### `X-Api-Key` header format for external API

`X-Api-Key: <prefix>.<secret>` (dot-separated)

---

## 20. Embed Tokens (Tenant + Public)

### Management (Bearer + `embed.<action>`)

| Method | Path                       | Permission                                            |
| ------ | -------------------------- | ----------------------------------------------------- |
| POST   | `/embed/tokens`            | create — `{ dashboardId, widgetId?, name?, ttlSec? }` |
| GET    | `/embed/tokens`            | view — `dashboardId, page, limit`                     |
| GET    | `/embed/tokens/:id`        | view                                                  |
| POST   | `/embed/tokens/:id/revoke` | delete — `{ reason? }`                                |

### Create response (secret returned ONCE)

```json
{
  "success": true,
  "data": {
    "token": {
      "id": "66a...",
      "name": "Marketing site",
      "dashboardId": "...",
      "widgetId": null,
      "expiresAt": "...",
      "status": "active",
      "createdAt": "..."
    },
    "secret": "abcdef123456..." // FULL TOKEN — only ever shown at creation
  }
}
```

### Public read — `GET /embed/:token`

- **Authentication:** **none** (token in path), `ci:routes-exempt`
- **CORS:** `Access-Control-Allow-Origin: *`, `credentials: false`
- **Rate limit:** 120 req/min per IP (configurable via `EMBED_RATE_LIMIT_*`)
- **Response 200:** executes the dashboard (or widget) — same shape as `GET /dashboards/:id/execute`
- **Errors:** 401 invalid/expired/revoked, 404 unknown — all opaque to prevent token enumeration

---

## 21. External API (`X-Api-Key`)

Mounted under `/external`. Authenticated by `X-Api-Key: <prefix>.<secret>`. Scopes enforced inline. Rate limit 60 req/min per IP.

| Method | Path                                  | Scope                                                                                                            |
| ------ | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| GET    | `/external/datasets`                  | `datasets:read` — query: `page, limit (<= 50)`                                                                   |
| GET    | `/external/datasets/:datasetId`       | `datasets:read`                                                                                                  |
| GET    | `/external/datasets/:datasetId/query` | `analytics:query` — query: `filters, filtersOp, dateRange, metrics, groupBy, orderBy, pagination` (JSON strings) |
| GET    | `/external/datasets/:datasetId/rows`  | `analytics:query` — `page, limit (<= 500)`                                                                       |
| GET    | `/external/dashboards/:dashboardId`   | `dashboards:read` (published only)                                                                               |

### Error codes

- `401 UNAUTHENTICATED` — missing/invalid/revoked/expired key
- `403 INSUFFICIENT_SCOPE` — key lacks required scope
- `404 NOT_FOUND` — dataset/dashboard not found or cross-tenant
- `422 VALIDATION_ERROR` — bad query params
- `429 RATE_LIMITED` — exceeded 60 req/min

---

## 22. Webhooks (Inbound, — Provider HMAC)

### `POST /webhooks/:webhookToken`

- **Authentication:** HMAC-SHA256 signature in `X-Saas-Signature: sha256=<hex>`, timestamp in `X-Saas-Timestamp: <unix-seconds>`
- **Body:** raw JSON bytes (signature computed over exact bytes)
- **Response 202:** `{ accepted: true, received: <count> }` — job processed via connector sync queue
- **Errors:** 401 bad signature, 422 invalid JSON

---

## Authentication Models

### User (tenant-portal actor)

```ts
type UserStatus = "active" | "invited" | "suspended" | "disabled";
interface User {
  _id: string;
  tenantId: string;
  email: string;
  status: UserStatus;
  profile: { name?: string; locale: string; timezone: string; avatarUrl?: string };
  mfaEnabled: boolean;
  roles: string[]; // role names
  permissions: string[]; // e.g. "dashboards.view"
  lastLoginAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

### Admin

```ts
type AdminType = "super_admin" | "platform_admin" | "support_admin" | "readonly_admin";
type AdminScope = { tenantId: string | "*"; allowed?: string[] };
interface Admin {
  _id: string;
  email: string;
  status: "active" | "suspended";
  adminType: AdminType;
  scope: AdminScope;
  mfaEnabled: boolean;
  roles: string[];
  permissions: string[];
}
```

### Role

```ts
interface Role {
  _id: string;
  name: string;
  description: string;
  scope: "platform" | "tenant";
  permissions: string[]; // e.g. ["dashboards.view", "dashboards.create"]
  isSystem: boolean;
}
```

### Permission

```ts
interface Permission {
  _id: string;
  module: string; // e.g. "dashboards"
  action: string; // e.g. "view"
  key: string; // `${module}.${action}`
  description: string;
  isSystem: boolean;
}
```

### Module

```ts
interface ModuleRecord {
  _id: string;
  key: string; // e.g. "dashboards" | "api_keys" | "embed"
  name: string;
  description?: string;
  parentKey?: string;
  isSystem: boolean;
}
```

---

## Stubs (501 Not Implemented) — DO NOT IMPLEMENT

The following routes return **501** today and should be shown as "Coming soon" in the frontend:

| Route                                    | Status                    |
| ---------------------------------------- | ------------------------- |
| `GET/POST/PUT/DELETE /settings/*`        | 501                       |
| `GET/POST/PATCH/DELETE /feature-flags/*` | 501                       |
| `POST /feature-flags/resolve`            | 501                       |
| `GET/POST/PATCH /email-templates/*`      | 501                       |
| `GET /monitoring/health/queue`           | 501                       |
| `GET /monitoring/health/scheduler`       | 501                       |
| `GET /monitoring/health/storage`         | 501                       |
| `GET /monitoring/health/connectors`      | 501                       |
| `GET /monitoring/metrics`                | 501 (Prometheus, Phase 4) |
| `POST /master-data/:catalogue/import`    | 501                       |
| `POST /master-data/:catalogue/export`    | 501                       |

---

## Known Limitations

1. **`npm audit`:** 2 moderate (transitive uuid < 11.1.1 via exceljs). Cannot fix without breaking exceljs upgrade; production not affected.
2. **Embed `/embed/:token` rate limit:** 120 req/min per IP — may need raising for high-traffic embedded dashboards.
3. **External API key rate limit:** 60 req/min per IP — per-key tracking is not enabled.
4. **No PDF reports:** JSON / CSV / XLSX only.
5. **No outbound email channel:** notifications are in-app + mailbox captured in dev (noop transport).
6. **Feature flags + Settings + Email templates:** read/write surfaces still 501 (services exist underneath, routes not wired).
7. **MongoDB connector tests:** real MongoDB connection not exercised in unit tests (validated via config only); integration requires a live MongoDB.

---

## Anything Lovable MUST NOT Assume

1. Do NOT create dashboard widget types other than: `kpi | table | bar | line | area | pie`.
2. Do NOT create connector types other than: `csv | xlsx | mongodb | webhook`.
3. Do NOT invent scopes for API keys; only the 4 listed are valid.
4. Do NOT invent actions for permissions; use the canonical action set or the platform will 422.
5. Do NOT treat the `/auth/login` response as proof of MFA completion — super_admin admins may need a second step.
6. Do NOT assume Settings/FeatureFlags/EmailTemplates are real — they return 501.
7. Do NOT assume `GET /connectors/types` returns more than the 4 registered providers.
8. Do NOT assume the embed route supports CORS with credentials — it never will.
9. Do NOT trust `configSummary` — secrets are never in it, even for webhook.
10. Do NOT create API keys without scopes — the backend rejects empty scopes.

---

## Final Verification

| Check                      | Result                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm test`                 | **405 tests, 405 pass, 0 fail**                                                                                                                                                                                                                                                                                                                                        |
| `npm run ci:guards`        | **5/5 OK** (check-stubs, check-routes, check-models, check-config, check-readme-sync)                                                                                                                                                                                                                                                                                  |
| `npm audit`                | **2 moderate** (transitive uuid in exceljs) — acceptable                                                                                                                                                                                                                                                                                                               |
| Backend fixes during audit | (a) Added `previewFile` / `syncFile` / `syncMongoDB` to connector controller, (b) Added `/connectors/:id/preview` and `/sync` (XLSX) + `/sync-mongodb` routes, (c) Fixed broken `apiKey.controller.js#updateApiKey` (was returning an error instead of updating) — now uses `apiKey.service#updateApiKey` which validates scopes and persists `name/scopes/expiresAt`. |

BACKEND VERIFIED — FRONTEND CONTRACT COMPLETE.
